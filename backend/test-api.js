import fetch from 'node-fetch';

async function main() {
  // We need an auth token to test the API route.
  console.log("We can't easily test API route without user token, but let's check the code of ChatController.acceptInvite");
}
main();
